<h1> Editing User Score Variable </h1>

<%= error_messages_for :user_variable %>

<%- form_for(:user_variable, :url => object_url, :html => { :method => :put }) do |f| %>
  <%= render :partial => "form", :locals => {:f => f} %>
  <p> <%= submit_tag "Update" %>
<% end %>
<%= link_to 'Show', object_path %>
|
<%= link_to 'Back', objects_path %>

<h1> Creating Shortcut</h1>

<%= error_messages_for :shortcut %>

<%- form_for(:shortcut, :url => objects_url) do |f| %>
  <%= render :partial => "form", :locals => {:f => f} %>
<% if !@shortcut.errors.blank? %>
  <p id="create_form"> <%= submit_tag "Create" %></p>
<% else %>
<p id="create_form" style="display:none;"> <%= submit_tag "Create" %></p>
<% end %>
  <% end %>

<%= link_to 'Back', objects_path %>
